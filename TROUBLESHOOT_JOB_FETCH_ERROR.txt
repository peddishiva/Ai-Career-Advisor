═══════════════════════════════════════════════════════════════════
  TROUBLESHOOTING: "Failed to fetch job recommendations"
═══════════════════════════════════════════════════════════════════

ERROR MESSAGE: "Failed to fetch job recommendations"

This error occurs when the frontend cannot connect to the backend API.

═══════════════════════════════════════════════════════════════════
🔍 DIAGNOSIS
═══════════════════════════════════════════════════════════════════

MOST COMMON CAUSE: Backend is not running

The job recommendations feature requires:
  1. ✓ Frontend running (http://localhost:3000)
  2. ✗ Backend running (http://localhost:5000) ← MISSING
  3. ? GEMINI_API_KEY environment variable set

═══════════════════════════════════════════════════════════════════
✅ QUICK FIX
═══════════════════════════════════════════════════════════════════

OPTION 1: Run the fix script
─────────────────────────────────────────────────────────────────
  Double-click: fix_job_recommendations.bat

OPTION 2: Start backend manually
─────────────────────────────────────────────────────────────────
  1. Open a new terminal/command prompt
  2. cd c:\Users\peddi\OneDrive\Pictures\Desktop\AiCarrerAvisor\backend
  3. python main.py
  4. Wait for "Application startup complete"
  5. Refresh your browser

OPTION 3: Use the all-in-one script
─────────────────────────────────────────────────────────────────
  Double-click: start_all_servers.bat

═══════════════════════════════════════════════════════════════════
🧪 STEP-BY-STEP DIAGNOSIS
═══════════════════════════════════════════════════════════════════

STEP 1: Check if backend is running
─────────────────────────────────────────────────────────────────
Open browser and go to:
  http://localhost:5000/health

Expected: {"status":"ok","service":"AI Career Advisor Backend"}

If you see:
  ✓ JSON response → Backend is running (go to Step 2)
  ✗ "This site can't be reached" → Backend is NOT running (see Fix below)

FIX: Start the backend
  Terminal 1:
    cd backend
    python main.py

STEP 2: Check if GEMINI_API_KEY is set
─────────────────────────────────────────────────────────────────
In the backend terminal, you should see:
  ✓ No errors about API key → Key is set
  ✗ "Gemini API key not configured" → Key is NOT set (see Fix below)

FIX: Set the API key
  PowerShell:
    $env:GEMINI_API_KEY="your-api-key-here"
    cd backend
    python main.py

  Command Prompt:
    set GEMINI_API_KEY=your-api-key-here
    cd backend
    python main.py

Get your key from: https://makersuite.google.com/app/apikey

STEP 3: Test the API endpoint directly
─────────────────────────────────────────────────────────────────
Open browser and go to:
  http://localhost:5000/api/jobs/recommendations?query=software%20engineer

Expected: JSON with job listings

If you see:
  ✓ JSON with jobs → API is working (go to Step 4)
  ✗ Error message → Check backend terminal for errors

STEP 4: Check browser console
─────────────────────────────────────────────────────────────────
1. Open your browser (http://localhost:3000/job-recommendation)
2. Press F12 to open Developer Tools
3. Go to Console tab
4. Look for error messages

Common errors:
  ✗ "Failed to fetch" → Backend not running
  ✗ "CORS error" → Backend CORS misconfigured
  ✗ "404 Not Found" → Wrong API endpoint
  ✗ "500 Internal Server Error" → Backend error (check backend terminal)

STEP 5: Check frontend is running
─────────────────────────────────────────────────────────────────
Open browser and go to:
  http://localhost:3000

Expected: AI Career Advisor homepage

If you see:
  ✓ Homepage loads → Frontend is running
  ✗ "This site can't be reached" → Frontend is NOT running (see Fix below)

FIX: Start the frontend
  Terminal 2:
    cd frontend
    npm run dev

═══════════════════════════════════════════════════════════════════
🔧 COMPLETE SETUP CHECKLIST
═══════════════════════════════════════════════════════════════════

□ Python installed (python --version)
□ Node.js installed (node --version)
□ Backend dependencies installed (pip install -r requirements.txt)
□ Frontend dependencies installed (npm install)
□ GEMINI_API_KEY environment variable set
□ Backend running on port 5000
□ Frontend running on port 3000
□ No firewall blocking localhost:5000
□ Browser console shows no errors

═══════════════════════════════════════════════════════════════════
🚨 COMMON ERRORS & SOLUTIONS
═══════════════════════════════════════════════════════════════════

ERROR 1: "Cannot connect to backend server"
─────────────────────────────────────────────────────────────────
CAUSE: Backend is not running
SOLUTION: Start backend with: python main.py

ERROR 2: "Gemini API key not configured"
─────────────────────────────────────────────────────────────────
CAUSE: GEMINI_API_KEY environment variable not set
SOLUTION: 
  1. Get key from: https://makersuite.google.com/app/apikey
  2. Set environment variable:
     PowerShell: $env:GEMINI_API_KEY="your-key"
     CMD: set GEMINI_API_KEY=your-key
  3. Restart backend

ERROR 3: "Server error: 500"
─────────────────────────────────────────────────────────────────
CAUSE: Backend Python error
SOLUTION:
  1. Check backend terminal for error messages
  2. Common issues:
     - Missing dependencies: pip install -r requirements.txt
     - Invalid API key: Verify your Gemini API key
     - API quota exceeded: Check Gemini API dashboard

ERROR 4: "Invalid response format from server"
─────────────────────────────────────────────────────────────────
CAUSE: Backend returned unexpected data
SOLUTION:
  1. Check backend terminal for errors
  2. Test API directly: http://localhost:5000/api/jobs/recommendations?query=test
  3. Verify Gemini API is responding correctly

ERROR 5: CORS error
─────────────────────────────────────────────────────────────────
CAUSE: Backend CORS not configured for frontend
SOLUTION:
  1. Check backend/main.py has:
     allow_origins=["http://localhost:3000"]
  2. Restart backend

ERROR 6: Port already in use
─────────────────────────────────────────────────────────────────
CAUSE: Another process is using port 5000 or 3000
SOLUTION:
  Windows:
    netstat -ano | findstr :5000
    taskkill /PID <PID> /F
  
  Then restart the server

═══════════════════════════════════════════════════════════════════
📝 VERIFICATION COMMANDS
═══════════════════════════════════════════════════════════════════

Check backend health:
  PowerShell:
    Invoke-WebRequest -Uri http://localhost:5000/health

Check jobs API:
  PowerShell:
    Invoke-WebRequest -Uri "http://localhost:5000/api/jobs/recommendations?query=test"

Check if port is in use:
  netstat -ano | findstr :5000
  netstat -ano | findstr :3000

Check environment variable:
  PowerShell: $env:GEMINI_API_KEY
  CMD: echo %GEMINI_API_KEY%

═══════════════════════════════════════════════════════════════════
✅ CORRECT STARTUP SEQUENCE
═══════════════════════════════════════════════════════════════════

1. Set GEMINI_API_KEY (one time)
   ─────────────────────────────────────────────────────────────
   PowerShell:
     $env:GEMINI_API_KEY="your-api-key-here"

2. Start Backend (Terminal 1)
   ─────────────────────────────────────────────────────────────
   cd c:\Users\peddi\OneDrive\Pictures\Desktop\AiCarrerAvisor\backend
   python main.py
   
   Wait for: "Application startup complete"

3. Start Frontend (Terminal 2)
   ─────────────────────────────────────────────────────────────
   cd c:\Users\peddi\OneDrive\Pictures\Desktop\AiCarrerAvisor\frontend
   npm run dev
   
   Wait for: "Ready on http://localhost:3000"

4. Test
   ─────────────────────────────────────────────────────────────
   Open: http://localhost:3000
   Click: Search
   Enter: "Software Engineer"
   Click: Search button
   
   Should: Redirect to job recommendations with AI-generated jobs

═══════════════════════════════════════════════════════════════════
🎯 EXPECTED BEHAVIOR
═══════════════════════════════════════════════════════════════════

When working correctly:

1. User searches for "Data Analyst" on /search page
2. Page redirects to /job-recommendation
3. Loading spinner appears
4. Console shows: "Fetching jobs from: http://localhost:5000/api/jobs/recommendations?query=Data%20Analyst"
5. Console shows: "Received jobs: {success: true, count: 8, jobs: [...]}"
6. 6-8 AI-generated job cards appear
7. Each job has: title, company, location, salary, description, tags

═══════════════════════════════════════════════════════════════════
📞 STILL NOT WORKING?
═══════════════════════════════════════════════════════════════════

1. Run: fix_job_recommendations.bat
2. Check both terminal windows for error messages
3. Check browser console (F12) for errors
4. Verify both servers are running:
   - Backend: http://localhost:5000/health
   - Frontend: http://localhost:3000
5. Try restarting both servers
6. Check if antivirus/firewall is blocking localhost

═══════════════════════════════════════════════════════════════════
